"""jev-acento — an independent, reproducible audit of Jev (TypeSafe AI) on Spanish.

Two things live here:

* the audit itself (fixed datasets, frozen prompts, a pre-registered decision rule), and
* ``acento compare``, which runs the same harness and metrics over your own labelled data.

See ``README.md`` for the questions being asked and ``PREREG.md`` for what was registered
before any data was collected.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]

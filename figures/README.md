# `figures/`

Regenerated by `make reproduce` from `runs/`. Do not edit; re-run instead.

- `reliability_paired_es.png` — confidence against observed accuracy per bin, all three arms
  overlaid, with bin counts annotated.
- `selective_accuracy_es.png` — accuracy against coverage, most-confident-first, with the
  coverage reached at `p_max ≥ 0.5` and `≥ 0.9` marked.
